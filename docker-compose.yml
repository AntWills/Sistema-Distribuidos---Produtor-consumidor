name: produtor-consumidor

services:
  cliente-back:
    image: cliente-back:latest
    build:
      context: ./cliente
      dockerfile: Dockerfile
    environment:
      - URL_LANCHONETE=localhosot:8081
    ports:
      - "8080:8080"

  lanchonete-back:
    image: lanchonete-back:latest
    build: 
      context: ./lanchonete
      dockerfile: Dockerfile
    ports:
      - "8081:8080"