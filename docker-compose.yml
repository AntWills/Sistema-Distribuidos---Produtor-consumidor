name: produtor-consumidor

services:
  cliente-back:
    image: cliente-back:latest
    build:
      context: ./cliente
      dockerfile: Dockerfile
    environment:
      - URL_LANCHONETE=http://lanchonete-back:8080
    ports:
      - "8080:8080"

  lanchonete-back:
    image: lanchonete-back:latest
    build: 
      context: ./lanchonete
      dockerfile: Dockerfile
    environment:
      - URL_FATURAMENTO=http://faturamento-back:8080
    ports:
      - "8081:8080"

  faturamento-back:
    image: faturamento-back:latest
    build:
      context: ./faturamento
      dockerfile: Dockerfile
    ports:
      - "8082:8080"